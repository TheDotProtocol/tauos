import ProjectGrayscalePage from '@/components/website/product/ProjectGrayscalePage';

export const metadata = {
  title: 'Project Grayscale | Tau',
  description: 'Explore the raw source patterns behind extreme scale networks. Security audited, publicly compiled, and free forever.',
};

export default function ProjectGrayscaleRoutePage() {
  return <ProjectGrayscalePage />;
}
