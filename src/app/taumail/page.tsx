import TauMailProductPage from '@/components/website/product/TauMailProductPage';

export const metadata = {
  title: 'Tau Mail | Tau',
  description: 'Email that keeps your conversations private. Zero access tracking, on-device spam detection, and end-to-end encrypted inbox storage.',
};

export default function TauMailPage() {
  return <TauMailProductPage />;
}
