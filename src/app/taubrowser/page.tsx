import TauBrowserProductPage from '@/components/website/product/TauBrowserProductPage';

export const metadata = {
  title: 'Tau Browser | Tau',
  description: 'The web, without the surveillance. Stop tracking scripts, load pages faster, and route traffic safely through built-in VPN.',
};

export default function TauBrowserPage() {
  return <TauBrowserProductPage />;
}
