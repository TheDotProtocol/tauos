import TauIDSettingsPage from '@/components/tauid/settings/TauIDSettingsPage';

export const metadata = {
  title: 'Settings | Tau ID',
  description: 'Manage your Tau ID profile and preferences.',
};

export default function Page() {
  return <TauIDSettingsPage />;
}
