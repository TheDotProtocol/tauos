import TauIDLoginPage from '@/components/tauid/auth/TauIDLoginPage';

export const metadata = {
  title: 'Sign in | Tau ID',
  description: 'Sign in to your Tau ID — one account for the entire Tau ecosystem.',
};

export default function Page() {
  return <TauIDLoginPage />;
}
