import PressPage from '@/components/website/marketing/PressPage';

export const metadata = {
  title: 'Press | Tau',
  description: 'The latest announcements, technical progress updates, and media assets from Tau OS.',
};

export default function Page() {
  return <PressPage />;
}
