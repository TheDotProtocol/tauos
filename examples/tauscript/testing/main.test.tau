fn test_addition() {
  let result = 2 + 2;
  print(result);
}

fn test_string_concat() {
  print("hello" + " world");
}

fn test_struct() {
  struct Point { x: 0, y: 0 }
  let p = Point { x: 1, y: 2 };
  print(p.x);
}
