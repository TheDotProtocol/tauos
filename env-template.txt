# TauOS Environment Variables Template
# Copy to .env.local for local development

# JWT Secrets (CHANGE IN PRODUCTION)
JWT_SECRET=tauos-super-secure-secret-key-2024-production-change-me
JWT_SECRET_TAUMAIL=tauos-taumail-jwt-secret-change-me
JWT_SECRET_TAUID=tauos-tauid-jwt-secret-change-me
# Optional master SSO secret (defaults to JWT_SECRET_TAUID)
JWT_SECRET_SSO=

# Database (Supabase session pooler recommended)
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# Supabase Storage (Tau Cloud — Phase 0)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=taucloud-files

# Mail transport — phone / local SMTP node (dev) or Vultr (prod)
MAIL_TRANSPORT=smtp
PHONE_MAIL_SERVER=true
PHONE_SMTP_HOST=127.0.0.1
PHONE_SMTP_PORT=2525
SMTP_HOST=127.0.0.1
SMTP_PORT=2525
SMTP_REALM=mail.tauos.org
PHONE_SMTP_LISTEN_HOST=0.0.0.0
PHONE_MAIL_NODE_NAME=phone-mail-node

# Tau AI — multi-model gateway (Phase 0)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
OPENAI_DEFAULT_MODEL=gpt-4o-mini
ANTHROPIC_DEFAULT_MODEL=claude-3-5-sonnet-latest

# Next.js app URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_APP_URL=http://localhost:3000

# Optional production fallback
SENDGRID_API_KEY=

# Environment
NODE_ENV=development
