# TauOS Environment Variables Template
# Copy to .env.local for local development

# JWT Secrets (CHANGE IN PRODUCTION)
JWT_SECRET=tauos-super-secure-secret-key-2024-production-change-me
JWT_SECRET_TAUMAIL=tauos-taumail-jwt-secret-change-me

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# Mail transport — phone / local SMTP node (recommended for dev)
MAIL_TRANSPORT=smtp
PHONE_MAIL_SERVER=true
PHONE_SMTP_HOST=127.0.0.1
PHONE_SMTP_PORT=2525
SMTP_HOST=127.0.0.1
SMTP_PORT=2525
PHONE_SMTP_LISTEN_HOST=0.0.0.0
PHONE_MAIL_NODE_NAME=phone-mail-node

# Next.js app URL (phone mail server posts inbound here)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_APP_URL=http://localhost:3000

# Optional production fallback
SENDGRID_API_KEY=

# Environment
NODE_ENV=development
